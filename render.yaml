services:
  - type: web
    name: smartbiz
    env: node
    plan: free
    buildCommand: npm install --prefix client && npm run build && npm --prefix server run render:install
    startCommand: node server/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
